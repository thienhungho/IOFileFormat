// Use from https://github.com/thienhungho/IOSpecification
export interface InformationObjectType {
    key: string
}

// Few data using to save data... we still can use full Information
export interface SimpleInformationObjectType {
    key: string,
}
